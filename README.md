This is a test repository. Don't mind me.

The repository now contains a minimal Vite-powered React wrapper so the stand-alone SPA can be built and deployed to GitHub Pages.

## Local development

```bash
npm install
npm run build
```
